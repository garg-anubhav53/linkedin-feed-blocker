# LinkedIn Feed Blocker

Chrome extension to hide the LinkedIn feed.

## Rules
- No frameworks, no build steps — vanilla JS only
- Minimal files: manifest.json, content.js, and nothing else unless necessary
- No options pages, popups, or settings unless asked
- CSS injection over DOM manipulation where possible
